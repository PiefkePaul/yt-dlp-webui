{
  "name": "yt-dlp-webui",
  "version": "1.1.0",
  "description": "Self-hostable yt-dlp download server with web interface and optional Docker packaging",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check-syntax.js",
    "smoke": "node scripts/smoke-test.js",
    "test:integration": "node scripts/integration-test.js",
    "pages:build": "node scripts/build-pages.js",
    "release:build": "node scripts/build-release.js",
    "verify": "npm run check && npm run smoke && npm run test:integration && npm run pages:build"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^4.21.2"
  }
}
